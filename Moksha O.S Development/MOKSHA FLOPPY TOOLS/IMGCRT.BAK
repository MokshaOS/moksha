#include<conio.h>
#include<fstream.h>
#include<bios.h>
void main()
{
clrscr();
char file_data[9216];
int i;
int head,track,starting_sector=1,sectors_to_read=17,drive=0;
cout<<"\t\t\tMoksha Operating System Image Creator";
cout<<"\n\nCreating Image.....";


for(head=0;head<=0;head++)
{
 for(track=0;track<=20;track++)
 {
  switch(head)
   {
    case 0:  // If Head is 0
     {
      switch(track)
      {
       case 0:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk0.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

       case 1:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk1.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	case 2:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk2.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	case 3:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk3.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	case 4:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk4.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

       case 5:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk5.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

      case 6:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk6.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

      case 7:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk7.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

      case 8:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk8.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

      case 9:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk9.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

      case 10:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk10.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

      case 11:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk11.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	case 12:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk12.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

       case 13:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk13.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

       case 14:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk14.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 15:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk15.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 16:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk16.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 17:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk17.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 18:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk18.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	case 19:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk19.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}

       break;}

	 case 20:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk20.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}
	 /*
	 case 21:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk21.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}


	 case 22:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk22.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 23:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk23.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 24:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk24.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 25:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk25.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 26:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk26.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 27:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk27.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 28:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk28.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 29:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk29.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 30:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk30.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 31:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk31.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 32:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk32.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 33:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk33.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 34:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk34.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 35:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk35.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 36:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk36.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 37:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk37.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 38:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk38.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 39:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk39.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 40:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk40.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 41:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk41.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 42:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk42.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 43:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk43.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 44:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk44.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 45:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk45.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 46:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk46.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 47:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk47.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 48:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk48.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 49:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk49.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 50:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk50.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 51:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk51.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 52:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk52.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 53:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk53.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 54:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk54.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 55:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk55.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 56:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk56.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 57:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk57.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 58:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk58.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 59:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk59.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 60:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk60.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 61:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk61.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 62:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk62.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 63:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk63.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 64:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk64.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 65:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk65.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 66:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk66.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 67:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk67.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 68:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk68.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 69:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk69.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 70:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk70.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 71:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk71.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 72:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk72.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 73:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk73.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 74:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk74.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 75:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk75.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 76:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk76.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 77:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk77.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 78:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk78.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;}

	 case 79:{
      biosdisk(_DISK_READ,drive,head,track,starting_sector,sectors_to_read,
	       file_data);
      ofstream writefile("H0Trk79.os",ios::binary);
      writefile.write((char *)&file_data,sizeof(file_data));
      writefile.close();
       for(i=0;i<=9215;i++)
       {
       file_data[i]=' ';}
       break;} */

      }// Switch for track block ends
   break;}

   }  // Switch for head block ends

}
}
cout<<"\n\nImage Created Successfully.";

getch();
}